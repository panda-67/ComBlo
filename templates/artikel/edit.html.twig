{% extends 'base.html.twig' %}

	{% block title %}Edit Artikel{% endblock %}

	{% block body %}
		<div class="bg-base p-7">
			<div class="flex justify-center items-center flex-col">
				<h1 class="text-xl font-bold">Form Edit Artikel</h1>
				{{ include('artikel/_form.html.twig', {'button_label': 'Update'}) }}
				{% if is_granted('ROLE_ADMIN') %}
					<div class="my-2">
						{{ include('artikel/_delete_form.html.twig') }}
					</div>
				{% endif %}
				<div class="">
					<a class="btn btn-sm btn-info btn-outline" href="{{ path('artikel_index') }}">back to list</a>
				</div>
			</div>
		</div>

	{% endblock %}
